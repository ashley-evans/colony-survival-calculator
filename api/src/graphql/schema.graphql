type FarmSize {
    width: Int!
    height: Int!
}

type Item {
    name: ID!
    createTime: Float!
    output: Int!
    size: FarmSize
}

type Query {
    item(name: ID): [Item]!
}

schema {
    query: Query
}
