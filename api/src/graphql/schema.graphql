interface BaseError {
    code: String!
}

type UserError implements BaseError {
    code: String!
    details: String
}

type FarmSize {
    width: Int!
    height: Int!
}

enum Tools {
    NONE
    STONE
    COPPER
    IRON
    BRONZE
    STEEL
    MACHINE
    NOGLASSES
    EYEGLASSES
}

type Item {
    id: ID!
    name: String!
    createTime: Float!
    output: Float!
    creatorID: String!
    creator: String!
    minimumTool: Tools!
    maximumTool: Tools!
    size: FarmSize
}

input CreatorOverride {
    itemID: String!
    creatorID: String!
}

type CreatorDemand {
    id: ID!
    name: String!
    amount: Float!
}

type RequirementCreator {
    id: ID!
    name: String!
    creatorID: String!
    creator: String!
    amount: Float!
    workers: Float!
    demands: [CreatorDemand!]!
}

type Requirement {
    id: ID!
    name: String!
    amount: Float!
    creators: [RequirementCreator!]!
}

type Requirements {
    requirements: [Requirement!]!
}

union RequirementResult = Requirements | UserError

type OptimalOutput {
    amount: Float!
}

union OutputResult = OptimalOutput | UserError

enum OutputUnit {
    SECONDS
    MINUTES
    GAME_DAYS
}

enum AvailableTools {
    NONE
    STONE
    COPPER
    IRON
    BRONZE
    STEEL
}

input OptimalFilter {
    maxAvailableTool: AvailableTools
    hasMachineTools: Boolean
}

input ItemsFilters {
    id: ID
    minimumCreators: Int
    creatorID: String
    optimal: OptimalFilter
}

type ItemName {
    id: ID!
    name: String!
}

type Query {
    distinctItemNames(locale: String): [ItemName!]!
    item(filters: ItemsFilters, locale: String): [Item!]!
    requirement(
        id: ID!
        maxAvailableTool: AvailableTools
        hasMachineTools: Boolean
        creatorOverrides: [CreatorOverride!]
        unit: OutputUnit
        workers: Int
        amount: Float
        locale: String
    ): RequirementResult!
    output(
        id: ID!
        workers: Int!
        unit: OutputUnit!
        maxAvailableTool: AvailableTools
        hasMachineTools: Boolean
        creatorID: String
    ): OutputResult!
}

schema {
    query: Query
}
