type FarmSize {
    width: Int!
    height: Int!
}

type Item {
    name: ID!
    createTime: Float!
    output: Int!
    size: FarmSize
}

type Requirement {
    name: ID!
    workers: Float!
}

type Query {
    item(name: ID): [Item]!
    requirement(name: ID!, workers: Int!): [Requirement]!
}

schema {
    query: Query
}
