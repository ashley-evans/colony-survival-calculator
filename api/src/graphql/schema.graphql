interface BaseError {
    code: String!
}

type UserError implements BaseError {
    code: String!
    details: String
}

type FarmSize {
    width: Int!
    height: Int!
}

enum Tools {
    NONE
    STONE
    COPPER
    IRON
    BRONZE
    STEEL
    MACHINE
    NOGLASSES
    EYEGLASSES
}

type Item {
    id: ID!
    name: String!
    createTime: Float!
    output: Float!
    creatorID: String!
    creator: String!
    minimumTool: Tools!
    maximumTool: Tools!
    size: FarmSize
}

input CreatorOverride {
    itemID: String!
    creatorID: String!
}

type CreatorDemand {
    id: ID!
    name: String!
    amount: Float!
}

type RequirementCreator {
    id: ID!
    name: String!
    creatorID: String!
    creator: String!
    amount: Float!
    workers: Float!
    demands: [CreatorDemand!]!
}

type Requirement {
    id: ID!
    name: String!
    amount: Float!
    creators: [RequirementCreator!]!
}

type Requirements {
    requirements: [Requirement!]!
}

union RequirementResult = Requirements | UserError

type OptimalOutput {
    amount: Float!
}

union OutputResult = OptimalOutput | UserError

enum OutputUnit {
    SECONDS
    MINUTES
    GAME_DAYS
}

enum AvailableDefaultTools {
    NONE
    STONE
    COPPER
    IRON
    BRONZE
    STEEL
}

input AvailableTools {
    default: AvailableDefaultTools
    machine: Boolean
    eyeglasses: Boolean
}

input ItemsFilters {
    id: ID
    minimumCreators: Int
    creatorID: String
    optimal: AvailableTools
}

type ItemName {
    id: ID!
    name: String!
}

input CalculateOutputInput {
    id: ID!
    workers: Int!
    unit: OutputUnit!
    availableTools: AvailableTools
    creatorID: String
}

type Query {
    distinctItemNames(locale: String): [ItemName!]!
    item(filters: ItemsFilters, locale: String): [Item!]!
    requirement(
        id: ID!
        availableTools: AvailableTools
        creatorOverrides: [CreatorOverride!]
        unit: OutputUnit
        workers: Int
        amount: Float
        locale: String
    ): RequirementResult!
    output(input: CalculateOutputInput!): OutputResult!
}

schema {
    query: Query
}
